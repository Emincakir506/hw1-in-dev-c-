#include <stdio.h>
int main()
{
	int temp,freezingpoint;
	printf("enter temp");
	scanf("%d" ,&temp);
	if(temp>0)
	printf("temp above the freezingpoint");
	else
	printf("temp below the freezingpoint");
	return 0;
}
